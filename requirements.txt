# Project Halide Dependencies
# Core
gradio>=5.0.0
torch>=2.0.0
torchvision>=0.15.0

# HuggingFace
transformers>=5.7.0
accelerate>=1.0.0
datasets>=2.0.0
huggingface_hub>=0.20.0
peft>=0.10.0

# Vision model (MiniCPM-V 4.6)
# torchcodec or av for video support
# flash-attn for fast attention (optional)

# Reasoning model (Nemotron-Mini-4B)
# No extra deps needed, uses transformers

# Training
deepspeed>=0.12.0
trl>=0.8.0
wandb>=0.15.0

# Data processing
pillow>=10.0.0
numpy>=1.24.0
opencv-python>=4.8.0
pyyaml>=6.0

# Storage
sqlite-utils>=3.0

# Modal (for training jobs)
modal>=0.50.0

# Utilities
tqdm>=4.65.0
requests>=2.28.0
